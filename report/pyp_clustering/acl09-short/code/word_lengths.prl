#!/usr/bin/perl -w
use Getopt::Std;
use File::Basename;
use List::Util qw(max maxstr min minstr reduce shuffle sum);
use lib "$ENV{HOME}/src/perl/";
use sg_utils;
use strict;
use vars qw();

my $usage = "Usage: $0 \n";

getopts('');

die $usage unless (1);

while (<>) {
chomp;
print length;
print "\n";
}

